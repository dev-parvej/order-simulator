# Smart Contract Configuration
CONTRACT_ADDRESS=0x... # Your deployed TradingSettlement contract address
BACKEND_PRIVATE_KEY=0x... # Private key for the backend wallet (must be set as backend address in contract)
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_PROJECT_ID

# MongoDB Configuration  
MONGODB_URI=mongodb://localhost:27017/order-balance-simulator

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration (optional)
CORS_ORIGIN=http://localhost:5173